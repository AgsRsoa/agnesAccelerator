import { TogglefavoriteDirective } from './togglefavorite.directive';

describe('TogglefavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglefavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
